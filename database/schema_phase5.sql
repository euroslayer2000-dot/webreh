SET NAMES utf8mb4;
-- ============================================================
--  Phase 5 : ผู้ดูแลระบบตั้งรหัสผ่านให้ผู้ใช้ใหม่โดยตรง (แทนลิงก์เชิญทางอีเมล)
--  - เพิ่ม must_change_password: บังคับให้ผู้ใช้เปลี่ยนรหัสผ่านทันทีหลังล็อกอิน
--    ครั้งแรก (ตอนที่ super_admin เป็นคนตั้งรหัสผ่านเริ่มต้นให้)
-- ============================================================
USE school_pr;

ALTER TABLE users
    ADD COLUMN must_change_password TINYINT(1) NOT NULL DEFAULT 0 AFTER is_active;
